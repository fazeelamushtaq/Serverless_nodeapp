config: 
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 100
  defaults: 
    headers: 
      Content-Type: "application/json"

scenarios:
  - name: "Getting All Products"
    flow:
      - get: 
          url: "/product"